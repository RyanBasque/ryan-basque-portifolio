import React, { Fragment } from "react";

import { HeaderTitle } from "./styles";

const HeaderBar = () => {
  return (
    <Fragment>
      <HeaderTitle>
        <b>Ryan</b>
        Basque
      </HeaderTitle>
    </Fragment>
  );
};

export default HeaderBar;
